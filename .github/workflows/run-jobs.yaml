name: Manually triggered workflow
on:
  workflow_dispatch:

jobs:
  build-matrix:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        number: [ 1 ]
    steps:
    - run: |
        echo "job number: ${{ matrix.number }}"
